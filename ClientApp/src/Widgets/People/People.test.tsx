import React from "react";
import ReactDOM from "react-dom";

describe("People", () => {
  it("displays loading indicator while handling form submission", async () => {
    // Verify condition...
  });

  it("render form inputs", async () => {
    // Verify form inputs are rendered
  });

  it("masks SSN input", async () => {
    // Verify text input for SSN is masked with XX-XX-XXXX format
  });
});
